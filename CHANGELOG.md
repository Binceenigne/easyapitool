# 更新日志

## 1.0.30 - 2026-08-07

- 调整图片生成思维链的模型与推理强度的上下限，低强度现在更快，高强度现在能力更强（也更贵）。
- 喝了一杯咖啡，优化了一点性能。

## 1.0.29 - 2026-08-06

- 修复流式过程图与最终成品图交接时偶发横跳、回到高模糊度重新播放的问题；最终结果现在原地同步已有图层，并从浏览器实际显示的清晰度继续过渡。
- 新增六档图片生成 Benchmark 窗口，可并行比较 Instant、Flash、Medium、High、Extra、Max 的结果、耗时、Token、调用次数与成本。
- 改进更新说明容错、下载状态反馈、思维用量流式展示和图片集多选控件，并保留原图缓存、续作、批量导出及窗口尺寸等现有功能。


## 1.0.28 - 2026-08-06

- 最新一轮生成图片优先加载并显示原图，历史轮次继续按需加载缩略图；恢复历史后也会保持这一规则。
- 右侧图片集新增复选框，支持多选批量导出原图和批量删除，并保留续作父子轮次的删除保护。
- 修复流式生成完成事件覆盖原图缓存的竞态，补充批量导出、取消导出和缺失原图提示。
- 延续页面缩放后的可用空间重排、等待状态文字主色脉冲和真实宽高比展示修复。

## 1.0.27 - 2026-08-06

- 修复移除等待背景动画后仍残留 Observer 清理调用，导致右侧图片工作流重绘、思维链展开、图片集展开和提交生成时抛出 `imageGenerationAnimationObserver is not defined` 并中断的问题。

## 1.0.26 - 2026-08-05

- 右侧等待生成与生成中卡片恢复纯白色背景，不再铺设思维深度按钮的流动背景动画。
- 等待状态文字会跟随左侧思维深度按钮的脉冲节奏，以当前档位主色柔和亮起；动画只在可见区域运行，并在结束后释放合成优化。

## 1.0.25 - 2026-08-05

- 修复页面缩小时内容无法填满窗口、放大时右侧或底部溢出的问题；内容区会按实际可用空间重新排版，标题栏保持固定，弹层定位也会同步适配。
- 图片排队和生成中的等待卡片复用当前思维深度按钮的复杂流动背景与动态色脉冲，并在离屏、折叠或窗口隐藏时暂停动画及释放合成优化，降低后台 CPU/GPU 开销。
- 流式过程图、最终缩略图和历史图片统一按真实宽高比以 100% 宽度完整展示，不再强制正方形；实时过程图会携带原始尺寸，减少比例跳变。
- 图片补边、等待动画和“生成中”状态标签支持深色模式，不再出现突兀的白色区域。
- 新增窗口尺寸记忆；应用重启或低开销模式重建界面后会恢复上次普通窗口宽高，同时跳过最大化、最小化和异常尺寸，并合并连续拖拽产生的写入。

## 1.0.24 - 2026-08-04

- 修复历史图片使用本地 `file:///` 地址时被 WebView2 拦截的问题；所有本地生成图会先通过应用桥接加载，不再产生“Not allowed to load local resource”错误。
- 生图费用在接口缺少实际 cost 时按 GPT-5.6 与 GPT Image 2 官方阶梯价格和真实 Token 用量估算，并明确区分实扣、估算与混合来源，不再使用账户总额差值归因。
- 页面缩放改用清晰文字重绘并修正缩放层尺寸补偿；生成按钮文字不再因合成层缩放发糊，动态背景光效也不会穿出按钮边界。
- 六档思维按钮统一使用自身主色柔光；悬停发光移至独立图层，High、Extra、Max 也会以 0.7 秒平滑渐入，不再出现白色实体边环或瞬间跳亮。
- 优化应用内更新下载反馈：点击下载后立即进入活动状态，真实文件进度继续按字节显示，同时增加持续移动的主色扫描动画；下载完成后动画停止并显示满格进度。

## 1.0.23 - 2026-08-04

- 生图思考面板新增 Token、Responses 调用次数和实际费用统计，悬停可查看输入与输出 Token 明细；统计会随图片历史持久化，重启后仍可查看。
- 修复思维链已经完成却仍显示“正在形成这一轮的判断”的问题；完成但没有正文的轮次会明确显示“本轮判断已完成”。
- 精简六档思维深度说明，取消文案前的“模式”字样并尽量保持单行展示；High 调整为更醒目的高饱和蓝紫色。
- 修复网络参考图地址含空格、括号、中文或已有百分号转义时可能下载失败的问题，重定向地址也会统一规范化。
- 加强网络图片地址安全与容错，拒绝 CR/LF、NUL、DEL 等控制字符；单个非法搜索结果不再中断整次生成。

## 1.0.22 - 2026-08-04

- 图片生成思维深度扩展为 Instant、Flash、Medium、High、Extra、Max 六档，并重新分配模型、推理强度、轮次和时间预算；Extra 沿用原 High 紫色效果，High 改为浅蓝紫。
- 思考耗时会随图片会话持久化，重启应用后历史记录不再归零；思维深度弹层改为贴近生成按钮显示。
- 历史图片按原始宽高比完整展示，不再强制裁切为正方形。
- 页面默认缩放调整为 100%，支持 Ctrl 加减缩放并自动保存；缩放仅作用于内容区，标题栏尺寸保持不变。

## 1.0.21 - 2026-08-03

- 修复流式过程图仍沿用旧版 22% 步长和 3px 模糊下限的问题；前三帧严格按 25%、50%、75% 递增，后续过程帧逐次折半剩余模糊度，最终成品帧到达 100% 清晰。
- 修复成品图清晰后旧过程图层仍因 10 秒淡入参与合成的问题；清晰化、图层接管和旧层清理统一在 3 秒内完成。
- 修正过程帧起点受网络到帧间隔影响而偏离计划区间的问题，并加强运行时模糊日志与静态资源版本核验。

## 1.0.20 - 2026-07-31

- 修复旧图片历史缺少思考轮次数据时，展开记录会白屏并导致图标消失的问题。
- 修复思维深度弹层覆盖底部状态文字的问题；展开后箭头会转为向下，再次点击同一按钮即可收起并复位箭头。
- 空提示词改用应用 Toast 提醒并自动聚焦输入框，不再显示浏览器原生校验气泡。

## 1.0.19 - 2026-07-31

- 重做图片生成思维深度选择器，提供 Instant、Flash、Medium、High、Max 五档横向滑块、档位说明和独立网络搜索开关；配置会自动保存，Flash 可快速判断任务与信息缺口后按需检索。
- 新增分轮次、可计时、可折叠的思考过程面板，默认显示六行并自动跟随流式内容；修复工具调用发生在流式输出中途时末尾文字可能被截断的问题。
- 优化选择器动画与性能：修复生成按钮圆角黑边，降低 High/Max 边缘发光强度；Max 滑条使用约 0.5 秒的外边框脉冲与外发光，并为按钮和选择器启用 GPU 合成优化。
- 修复生成按钮右侧思维深度入口打开后弹层被父级裁切、看起来无法点击的问题，并改善 260px 起的小窗口布局。
- 增强 Windows 静态资源缓存发布，在短暂访问拒绝时自动重试，减少更新或首次启动时资源初始化失败。

## 1.0.18 - 2026-07-31

- High 与 Max 思维模式新增按需联网搜索，可在同一轮并行或跨轮多次检索网页与视觉候选、核验缩略图并选择最多 3 张网络参考图参与生成；搜索失败时会继续使用现有信息完成创作。
- 思维面板新增联网检索状态、候选数量和已采用网络参考图数量；点击采用数量可展开查看所有最终纳入的图片，历史图片集也会保留引用图片与来源信息。
- 修复思维模式会把用户明确指定的现有 IP 或角色主动改写为原创替代的问题；现在会保留角色身份、所属设定和标志性特征，除非用户明确要求原创化。
- 优化流式缩略图预览，前三张过程图每张减少 22% 初始模糊度；接口返回额外过程图时继续展示，并按 11%、5.5% 等折半步长平滑降低模糊度。

## 1.0.17 - 2026-07-30

- 新增 GPT Image 2 生图与图片编辑工作区，支持参考图、批量生成、续作会话和多种输出预设。
- 新增提示词一键润色与多档 ReAct 思维模式，可查看并复制原始提示词、工作摘要和优化后的最终提示词。
- 新增三阶段流式缩略图预览，首张过程图以 5 秒渐入并同步执行 10 秒高斯模糊，中间图平滑接力，最终图以 3 秒动画变为清晰。
- Max 思维模式生成按钮改为高饱和紫罗兰色，并加入轻柔流动光泽。
- 新增图片集历史记录、默认折叠、按需加载、手动展开折叠、继续编辑和删除功能；最新图片集保持默认展开。
- 优化生成结果内存占用，折叠历史不再预读图片，最终动画完成后只保留最终图片图层。
- 修复流式响应结束时可能误用中间图、图片文件夹按钮被后台 RPC 拒绝及多处小窗口布局问题。

## 1.0.16 - 2026-07-23

- 修复代理环境下下载更新时出现 TLS 连接中断的问题。
- 更新文件会自动尝试系统代理、直连和多个下载地址，并支持断点续传。
- 下载过程中会持续显示真实进度，网络中断后重试不会丢失已经下载的部分。
- 下载失败时不再显示复杂的 SSL 错误，而是提示检查网络或代理设置。

## 1.0.15 - 2026-07-23

- 修复部分代理环境下检查更新长时间停留在“正在检查”的问题。
- GitHub 连接异常时会快速切换系统代理、直连和备用检查通道，并在有限时间内返回结果。
- 修复 TLS 连接被提前关闭时显示技术性错误的问题，失败提示现在更清晰。

## 1.0.14 - 2026-07-23

- 将默认和极简标题栏的置顶按钮移到右上角，并放在最小化按钮左侧。
- 置顶按钮不再显示背景悬停效果，悬停和置顶状态只通过图钉颜色提示。

## 1.0.13 - 2026-07-23

- 新增窗口置顶功能，方便在查看或操作其他应用时持续关注用量。
- 默认和极简标题栏可从左侧图钉按钮切换置顶；Windows 原生标题栏可从设置按钮右侧切换。
- 置顶状态会自动保存，进入低开销模式后重新打开窗口仍然有效。
- 修复 GitHub 接口访问次数用尽时无法检查更新的问题，应用会自动切换到备用检查方式。

## 1.0.12 - 2026-07-23

- 检查更新时只显示尚未安装版本的更新内容，不再一次展示全部历史记录。
- 更新窗口新增“查看完整更新日志”，需要时可以展开查看完整更新路线。
- 优化更新说明，用更直白的语言说明每项变化及其影响。

## 1.0.11 - 2026-07-23

- 应用行为新增三种后台运行方式，可按内存占用和打开速度自由选择。
- 默认在隐藏 5 分钟后进入低开销模式，再次打开窗口时会重新加载界面。
- 低开销模式可降低约 80% 的后台内存和运行开销，但再次打开窗口会稍慢。
- “始终保持活跃”会保留界面，重新打开速度更快，但后台内存占用较高。
- 无论选择哪种模式，后台都会继续自动获取用量并保存历史数据。

## 1.0.10 - 2026-07-22

- 修复了点击刷新按钮有概率失效的问题。
- 修复了添加、删除、设置等其他按钮偶尔点击无响应的问题。
- 优化后台运行逻辑，隐藏窗口后可以释放界面占用的内存。
- 修复设置页面被顶部按钮遮挡的问题。

## 1.0.9 - 2026-07-22

- 修复部分网络环境下无法检查更新的问题。
- 刷新按钮点击后立即进入 5 秒冷却，避免重复刷新。
- 刷新按钮新增加载、成功、失败和冷却倒计时反馈。
- 增加刷新过程日志，方便排查刷新失败或按钮无响应的问题。

## 1.0.8 - 2026-07-20

- 修复更新完成后应用偶尔无法自动重新启动的问题。
- 修复最小化到托盘时应用偶尔无响应的问题。
- 修复部分标题栏按钮偶尔点击无响应和窗口恢复时闪烁的问题。
- 优化密钥选择菜单，支持键盘操作并统一不同标题栏下的体验。
- 调整极简标题栏的尺寸和按钮大小，操作更方便。

## 1.0.7 - 2026-07-17

- 修复应用重新启动后偶尔出现页面文件丢失的问题。
- 优化极简标题栏的高度、图标和窗口按钮大小。
- 极简模式支持更小的窗口尺寸。

## 1.0.6 - 2026-07-17

- 新增默认、极简和 Windows 原生三种标题栏样式。
- 极简标题栏会收起常用操作，为内容留出更多空间。
- Windows 原生标题栏支持系统自带的拖动、缩放和窗口按钮。

## 1.0.5 - 2026-07-16

- 优化更新下载进度显示。
- 修复下载完成后偶尔无法继续安装的问题。
- 更新完成后可以选择稍后重启或立即重启。
- 修复无限额度账户可能显示错误额度上限的问题。

## 1.0.4 - 2026-07-16

- 新增自动检查更新和应用内更新提示。
- 支持暂时跳过或忽略指定版本。
- 用量趋势图新增 1 小时和 10 分钟两种查看方式。
- 修复正常用量变化可能被误报为额度上限调整的问题。
- 更新应用图标和 Windows 通知图标。

## 1.0.3 - 2026-07-16

- 修复检查或下载更新时可能长时间无响应的问题。
- 修复自定义提醒阈值保存后显示不一致的问题。
- 额度上限或频率限制变化时会发送 Windows 通知。
- 优化应用和通知图标。

## 1.0.2 - 2026-07-16

- 默认余量提醒调整为 50%、25% 和 10%。
- 优化更新进度显示和更新日志排版。
- 不同提醒等级使用更容易区分的通知图标。

## 1.0.1 - 2026-07-15

- 修复用量超过限制时剩余额度可能显示为负数的问题。
- 改进剩余额度和已用比例的显示规则。

## 1.0.0 - 2026-07-15

- 新增自动检查更新、下载和安装更新功能。
- 新增关闭按钮行为和 Windows 开机启动设置。
- 新增应用内更新日志。
- 优化小窗口下的进度显示和界面动画。
